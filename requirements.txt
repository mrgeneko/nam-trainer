# NAM Trainer dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
neural-amp-modeler[cli]>=0.9.0

# GUI (tkinter comes bundled with Python on most platforms)
# On some Linux distros, you may need: apt install python3-tk
